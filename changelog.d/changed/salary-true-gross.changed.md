- Resolve the `salaire_brut` dual-convention (spec 0001): the salary input is now **true gross**. A flat-rate employee-cotisations layer (`cotisations_salariales`, ≈ 11,31 % for 2024, excl. CSG/CRDS) derives the declared salary (`salaire_declare`, case 1AJ) that the income-tax chain uses, and the CSG déductible is now deducted exactly once. `revenu_disponible` subtracts the cotisations, and CSG/CRDS stay on 98,25 % of gross. Adds a payslip-identity property test; the DGFiP oracle rows are unchanged (declared → IR is preserved).
