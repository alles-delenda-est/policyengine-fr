- Replace the flat employee-cotisations rate (spec 0001) with a **per-risk, PASS-bracketed** schedule (spec 0004): vieillesse plafonnée/déplafonnée, AGIRC-ARRCO retraite + CEG tranche 1/2, and CET/APEC for cadres (via a new `cadre` input), with a new `plafond_securite_sociale` (PASS) parameter. Identical to the old flat rate at/below 1 PASS; correctly lower above it (vieillesse plafonnée stops at the PASS) and higher for cadres. `salaire_declare` and downstream outputs shift only for earners above the PASS.
