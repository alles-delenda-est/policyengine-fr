Hardened validation of the modelled benefits: AF edge-case tests (BMAF 1-April revalorisation, age 0/14/19/20 boundaries, modulation-plafond brackets, 4 children), CSG déplafonnement and decomposition cases, and pytest property invariants (AF non-negativity / zero-below-two-children / monotonic-in-resources; CSG and assiette identities). Added an independent-oracle harness (`tests/test_oracle.py` + `tests/fixtures/oracle_values.yaml`) that compares the model to the French government's own simulators and skips uncaptured rows. Captured the DGFiP income-tax simulator (revenus 2024) for a 10-household battery — all match `impot_revenu` to within €0.50, independently validating the barème, quotient-familial parts, plafonnement du QF, and décote. Made the AF base-ressources N-2 simplification explicit in the variable docstring and `modelled_policies.yaml`.
