- Add the **revenu de solidarité active (RSA) socle** (spec 0002), the first means-tested benefit. Models the montant forfaitaire equivalence scale (couple + per-child majorations, and the RSA majoré for a single parent), the forfait logement, and a differential base ressources (salary, pensions alimentaires, AF, ASF). `RSA = max(montant forfaitaire − forfait logement − base ressources, 0)`, wired into `revenu_disponible`. All 2024 amounts transcribed from the Décret 2024-396 and CASF R262-1/R262-9; official CAF-estimateur oracle rows are staged skip-until-captured.
