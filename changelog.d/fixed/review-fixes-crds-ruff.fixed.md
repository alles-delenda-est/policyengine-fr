- Fix the CRDS on retirement pensions at the reduced (3,8 %) CSG band (spec 0006 review): CRDS 0,5 % is due from the taux réduit up — only the taux-zéro exoneration is CRDS-free — so `crds_retraite` now levies it whenever CSG is due (`taux_csg > 0`), with a réduit-band test row added. Also pins ruff (`==0.15.20`) in CI and the dev dependency so a floating version can't reformat unrelated files and break Lint.
